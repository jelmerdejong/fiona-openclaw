# Daily Note

**Harvester run (07:08 UTC):** Scanned cron:session-harvester session. Only harvester setup instructions found — no user-generated content to process. No new durable facts extracted.

**Harvester run (07:11 UTC):** Scanned cron:session-harvester again. No active user sessions. Only this harvester session exists (11 lines, internal setup/execution only). No new durable facts extracted.

**Harvester run (07:12 UTC):** No transcript files accessible; lastRunAt nearly current. No new durable facts extracted.

**Harvester run (08:00 UTC):** Transcript files inaccessible due to exec permissions. Updated state and logged. No new durable facts extracted.

**Harvester run (10:00 UTC):** Transcript files inaccessible due to exec allowlist restriction. Updated state. No new durable facts extracted.
# Daily Note

**Harvester run (07:08 UTC):** Scanned cron:session-harvester session. Only harvester setup instructions found — no user-generated content to process. No new durable facts extracted.

**Harvester run (07:11 UTC):** Scanned cron:session-harvester again. No active user sessions. Only this harvester session exists (11 lines, internal setup/execution only). No new durable facts extracted.

**Harvester run (07:12 UTC):** No transcript files accessible; lastRunAt nearly current. No new durable facts extracted.

**Harvester run (08:00 UTC):** Transcript files inaccessible due to exec permissions. Updated state and logged. No new durable facts extracted.

**Harvester run (10:00 UTC):** Transcript files inaccessible due to exec allowlist restriction. Updated state. No new durable facts extracted.

## Manual Session — Harvester Fix & Exec Policy (06:58–11:17 UTC)

### Harvester Debugging
- Jelmer noticed session-harvester cron ran but nothing from yesterday's conversations (Obsidian research, setup work) made it to memory.
- **Root cause:** Haiku cron sessions used `exec` to read transcript files → requires approval → nobody at 4am → silent failure with timestamp bump (illusion of working).
- **Manual rescue:** Read entire Day 1 transcript (890ba2b3...jsonl.reset, 275 lines, 687KB). Extracted all durable facts to MEMORY.md and wrote proper 2026-04-01.md summary.
- **Harvester rewritten 3 times:**
  1. v1: Used `sessions_list` + `read` only → `sessions_list` from isolated session can only see itself
  2. v2: Tried `read` on directories → `read` tool can't list directories (EISDIR)
  3. v3 (final): Uses `exec ls` for directory listing + `read` for file contents — works now that `ask=off`

### Exec Policy Saga
- Jelmer set `openclaw config set tools.exec.ask off` at ~07:11 UTC → harvester started working
- **BUT**: `tools.profile: "coding"` imposes an **allowlist** on top of `ask` setting. Commands not on allowlist get "allowlist miss" denial regardless of `ask=off`.
- Discovered `tools.profile` options: `minimal`, `coding`, `messaging`, `full`
- Jelmer switched to `tools.profile: "full"` at ~11:14 UTC
- **Still blocked as of 11:17 UTC** — gateway restart may not have completed, or session caching old policy. Awaiting Jelmer to confirm `openclaw gateway restart`.

### Git Backup
- 06:00 UTC backup failed (ran before `ask=off` was set)
- Manual trigger at ~11:05 UTC reported `ok` in 3.5s but suspiciously fast — no commit appeared on GitHub
- Likely same allowlist issue — Haiku said "done" without actually running git commands
- **Needs verification** once exec policy is truly fixed

### Key Learnings
- Haiku will happily report success even when it can't do the work — always verify outputs, not just status codes
- `tools.profile` overrides `exec.ask` — allowlist takes precedence
- `read` tool cannot list directories; `sessions_list` from isolated sessions has limited visibility
- Isolated cron sessions are sandboxed differently than main session

**Harvester run (12:00 UTC):** Transcript files inaccessible (exec allowlist still blocking). State updated. No new durable facts extracted.
